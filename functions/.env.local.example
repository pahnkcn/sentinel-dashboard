# Copy to .env.local to override these optional emulator defaults.
OPENROUTER_MODEL=z-ai/glm-5.2
# Required exact provider endpoint slug approved for synthetic emulator tests.
OPENROUTER_PROVIDER=replace-with-reviewed-provider-slug
